import random 	#random num generator
import sys	#sys module 
import os	#OS module

'''
Tuples >> Similar but unlike Lists cannot be changed after created

'''

pi_tuple = (3,5,4,8,9,10)
new_tuple = list(pi_tuple)
new_list = tuple(pi_tuple)

print(len(new_list))
print(min(new_list))
print(max(new_list))

